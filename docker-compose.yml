version: "3.9"

services:
  php-fpm:
    build: ./docker/php-fpm
    volumes:
      - ./www:/var/www/
    ports:
      - "9000"
    links:
      - mysql

  nginx:
    build: ./docker/nginx
    volumes:
      - ./www:/var/www/
    ports:
      - "80"
    links:
      - "php-fpm:php"

  mysql:
    build: ./docker/mysql
    environment:
      MYSQL_ROOT_PASSWORD: secret
      MYSQL_DATABASE: mydb
      MYSQL_USER: myuser
      MYSQL_PASSWORD: password
    volumes:
      - ./data/mysql:/var/lib/mysql
